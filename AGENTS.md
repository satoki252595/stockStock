# 環境構築・依存ライブラリ管理

- 環境構築とライブラリ管理はプロジェクトのnix（`flake.nix`）を使う。
- nixが不可能または既存方針と衝突する場合は、独断で切り替えずユーザーに確認する。
- `pip install` / `npm install -g` / `brew install` / `cargo install`等のグローバル・ユーザーランドへの直接インストールを避け、nixでの解決を優先する。

# プロジェクト単位のGitHub管理（ユーザー確定ルール）

- 開発の有無を問わず企画段階から、各プロジェクトを専用GitHubリポジトリで管理する。stockStockは既存の `satoki252595/stockStock` を再利用し、公開範囲を勝手に変更しない。
- コード、企画、調査と出典、要件、デザイン、事業計画、集客・販売、運用、検証結果、意思決定を同じリポジトリに残す。作業はIssues、変更・判断はコミットや必要に応じPRで追跡する。
- 新規着手時は既存リポジトリを確認し、なければ専用リポジトリを用意する。公開範囲の指定がなければprivate。既存の重複作成・一括移行はしない。
- 外部ドキュメント・デザインも、編集可能な書き出しまたは参照URL・更新日・決定内容を保存する。秘密情報と顧客個人情報はGitに保存しない。
- プロジェクト開始時に本ルールを各repoのAGENTS.mdへ引き継ぐ。システムを作らない案件はコード雛形を作らず必要な資料から始める。
- 変更時はREADMEとコミット本文に更新者・日時・依頼者・目的・検証結果を記録する。

# サービス単位のローカルフォルダ（ユーザー確定ルール）

- サービスごとに独立したフォルダを専用GitHubリポジトリに対応させる。新規の標準配置は `/Users/satoki252595/projects/<service-slug>/`。Office配下に別サービスを作らない。
- コード以外の企画・調査・デザイン・販売・運用資料もサービスのフォルダへ置く。Officeは横断台帳・共通手順・候補比較と参照を置く。
- 既存の専用フォルダを再利用し、ルール追加を理由に一括移動・改名しない。同一サービスの作業分離にはworktreeを利用できる。
