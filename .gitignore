# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# 収集データ（原本は Notion ⑤ に保存。ローカルは作業領域）
data/raw/
data/export/

# ローカル設定・資格情報
.env
db_ids.local.json

# nix
.direnv/
result

# codegraph ツール生成物
.codegraph/

# 配信 Worker (worker/) の依存。pnpm で入れるものでリポジトリには入れない。
# ブランチごとの worker/.gitignore に頼るとブランチを切り替えたときに漏れる
# （実際に 9,518 ファイル・70MB を1度コミットしてしまった）ので、ルートで止める。
node_modules/
worker/node_modules/
